package org.atcgroup.starlight.bundle.mission.commission;

import java.util.UUID;

public class ManualCommission extends Commission {
    public ManualCommission(UUID uuid, UUID creator) {
        super(uuid, creator);
    }
}
