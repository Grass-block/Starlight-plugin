package org.atcgroup.starlight.bundle.task.data;

import java.util.UUID;

public class ManualCommission extends Commission {
    protected ManualCommission(UUID uuid, UUID creator) {
        super(uuid, creator);
    }
}
